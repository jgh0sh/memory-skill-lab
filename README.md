# Memory Skill Lab

This repository is the memory skill lab. It is a playground for the memory skill defined in `memory-skill/SKILL.md`.

## Contents

- `memory-skill/SKILL.md` contains the session memory bootstrap skill.
- `memories.md` stores the YAML memory block the skill reads and writes.
- `app.py` is a simple Flask image resize service used for experimentation.